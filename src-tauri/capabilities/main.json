{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "The settings window calls this app's own commands (not permission-gated) and listens for one event. It reaches no plugin and no other core API — autostart, the global shortcut, the updater and the tray are all driven from Rust.",
  "windows": ["main"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
